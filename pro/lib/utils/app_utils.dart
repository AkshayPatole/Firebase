import 'package:pro/constants/app_enum.dart';
import 'package:pro/constants/app_images.dart';

String fetchIconFromAssets(assetname) {
  if (assetname == AppIcons.name.name) {
    return AppImages.home;
  } 
  // else if(assetname==AppIcons.values){
    
  // }
  return "";
}
